def square(x):
    """This program checks if the code may have an issue using the 
        assert function if none no output given if there is a bug 
        an output is given"""
    return x * x

assert square(10) == 100 